import 'package:flutter/material.dart';

class ServiceAttendancePage extends StatelessWidget {
  const ServiceAttendancePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
