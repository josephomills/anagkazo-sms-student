import 'package:flutter/material.dart';

class DisciplinaryPointsPage extends StatelessWidget {
  const DisciplinaryPointsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset("assets/illustrations/welcome.png"),
    );
  }
}
