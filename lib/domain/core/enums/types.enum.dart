enum EventCategory { academic, church, other }

enum EventType {
  vision,
  pillar,
  foundational,
  tutorial,
  live,
  experience,
  encounter,
  assembly,
  other,
}

enum ScanType { scanIn, scanOut }
